apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.observability.namespace }}
  labels:
{{- range $key, $value := .Values.observability.labels }}
    {{ $key }}: {{ $value | quote }}
{{- end }}
