apiVersion: tf.upbound.io/v1beta1
kind: Workspace
metadata:
  name: {{ .Values.name }}-infrastructure
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  providerConfigRef:
    name: {{ .Values.name }}
  forProvider:
    source: Remote
    module: git::https://github.com/juanroldan1989/platform.git//terraform/modules/civo_cluster?ref=main
    vars:
    - key: cluster_name
      value: {{ .Values.name }}
    - key: k8s_version
      value: {{ .Values.k8s_version }}
    - key: node_count
      value: {{ .Values.node_count | quote }}
    - key: node_type
      value: {{ .Values.node_type }}
