# TODO: Only for prototyping, will be replaced with Sealed Secrets in production.
apiVersion: v1
kind: Secret
metadata:
  name: crossplane-secrets
  namespace: crossplane-system
  annotations:
    argocd.argoproj.io/sync-wave: "5"
type: Opaque
stringData:
  CIVO_TOKEN: "CIVO_TOKEN"
  TF_VAR_civo_token: "CIVO_TOKEN"
